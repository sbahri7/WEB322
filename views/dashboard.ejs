<div class="container mx-auto mt-8">
  <h2 class="text-2xl font-bold mb-2">Welcome, <%= user.userName %>!</h2>
  <p>Email: <%= user.email %></p>
  <h3 class="mt-4 font-bold">Login History:</h3>
  <ul class="list-disc pl-4">
    <% user.loginHistory.forEach(item => { %>
      <li><%= item.dateTime %> — <%= item.userAgent %></li>
    <% }) %>
  </ul>
  <a href="/logout" class="btn btn-error mt-4">Logout</a>
</div>
